package com.huang;

import java.util.Arrays;
import java.util.LinkedHashMap;

/**
 * Created by hlp on 2017/5/3.
 */
public enum Ec {
    INSTENCE;
    public void f(){
        int[] a=new int[10];
         a = Arrays.copyOf(a, a.length * 2);
        Object[] objects=new Object[10];
        new LinkedHashMap();
        Arrays.copyOf(objects,objects.length+10);
        System.out.println("..........");
    }
}
